query ProductsGetList($take: Int!, $offset: Int!) {
	products(first: $take, skip: $offset) {
		id
		name
		description
		categories(first: 1) {
			name
		}
		images(first: 1) {
			url
		}
		price
	}
}
